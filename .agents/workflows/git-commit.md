---
description: 変更内容をステージングしてコミットします
---

// turbo-all

1. すべての変更をステージングに追加します
   git add .

2. 現在の変更を元に適切なメッセージを作成し、コミットします
   git commit -m "update: 修正内容を反映"

3. 変更内容をリモートリポジトリ（GitHub）に送信します
   git push
