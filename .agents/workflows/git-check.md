---
description: gitの変更状態を自動で確認します
---

// turbo-all

1. 作業ツリーの状態を確認します
   git status

2. 変更内容（差分）を表示します
   git diff

3. ステージング済みの変更内容を表示します
   git diff --cached

4. 追跡されていないファイルを確認します
   git ls-files --others --exclude-standard
